using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

namespace StretchySurgeons {
    public class Target // : Entity
    {
		    /*
    private Vector2Int targetTile;

        void Start(){
            //TODO: set size as a grid prop
            targetTile = new Vector2Int(Random.Range(-5, 5), Random.Range(-5, 5));
            transform.position = new Vector3(targetTile.x * Constants.TILE_SIZE, targetTile.y * Constants.TILE_SIZE, transform.position.z);;         
        }

        // Dumb collision detection by simply checking if the current tile is occupied
        void Update(){
            if (grid.IsTileOccupied(targetTile)){
                resetTarget();
            }
        }

        private void resetTarget(){
            targetTile.x = Random.Range(-5, 5); //TODO: set size as a grid prop
            targetTile.y = Random.Range(-5, 5);
            transform.position = new Vector3(targetTile.x * Constants.TILE_SIZE, targetTile.y * Constants.TILE_SIZE, transform.position.z);;
        }

        public override bool IsOccupyingTile(Vector2Int tile) {
			return false;
		}
      */

    }

}